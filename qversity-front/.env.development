# Local (Vite mode: development)
# 실행: npm run dev
# 모바일: 같은 Wi-Fi에서 http://<PC_IP>:4000

# 비우면 API/OAuth가 Vite 프록시(동일 출처)를 탐 → IP 접속 가능
VITE_API_BASE_URL=
# Vite 서버가 PC의 백엔드로 넘기는 주소
VITE_PROXY_TARGET=http://127.0.0.1:18080
VITE_DEV_PORT=4000
# true → 0.0.0.0 바인딩 (LAN/모바일 접속)
VITE_HOST=true
